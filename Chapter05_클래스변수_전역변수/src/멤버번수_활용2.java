/*
 *		달력 만들기
 *		1. 년도 / 월 year / month
 *		2. 요일 => year / month / week
 *		3. 출력 = year / month / week
 */
// 멤버 변수 없이 메서드 => 리턴 형, 매개 변수만 사용
/*import java.time.Month;
import java.util.*;

public class 멤버번수_활용2 {
	static int year, month, day, week;
	static int  input(String msg) {
		Scanner scan = new Scanner(System.in);
		System.out.println(msg + " 입력 : ");
		return week;
	}
	
/*	//입력
	static int input(String msg) {
		Scanner scan = new Scanner(System.in);
		System.out.println(msg +" 입력 : ");
		return scan.nextInt();
	}
	*/
/*	static int getWeek() {
		Calendar cal = Calendar.getInstance();
		cal.set(Calendar.YEAR
		return week;
	}
	static void print() {
		
		
	}
	static int getWeek(int year, int month, int day) {
		Calendar cal = Calendar.getInstance();
		cal.set(Calendar,year,  month);
		cal.set(Calendar,Month, month - 1);
		cal.set(Calendar,date, day - 1);
		
		int week = cal.get(Calendar.DAY_OF_WEEK);
	}
	static void print("%d년도 %d월 %d일 %s요일입니다\n ", year, month, day, strweek[week]);
	
	public static void main(String[] args) {
		year = itput("년도");
		month = itput("월");
		day = itput("일");
		week = itput("요일");

	}

}
*/
